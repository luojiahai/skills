{
  "name": "archiver-skill-douyin",
  "version": "1.0.0",
  "private": true,
  "description": "Dependencies for the archiver skill's Douyin platform. Kept skill-local so the skill is portable and the host repo stays clean. Install with ../../setup.sh douyin.",
  "type": "module",
  "dependencies": {
    "playwright": "^1.62.1"
  }
}
