---
"luojiahai-skills": patch
---

archiver: a rename on a run with nothing left to fetch now completes all three writes — the folder, `account.json` inside it, and `archiver.json`. Moving the folder alone left the archive naming the folder it had just left.
