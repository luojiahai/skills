---
"luojiahai-skills": minor
---

archiver (Douyin): a re-run stops scrolling once it recognises 20 consecutive posts it already has, instead of scrolling the whole profile every time. `--full` collects the whole profile anyway, and a listing that stopped early withholds the counts that can only be worked out from a complete one.
