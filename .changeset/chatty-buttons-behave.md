---
"luojiahai-skills": patch
---

preparing-tax-return: simplify document gathering. The opening interview no longer asks where your records live — batch 0 is now just the spouse question. Step 3 instead walks the document list from step 2 and asks for one document at a time, taking it however you hand it over: pasted into the session, a file dragged in, a path, a link, or dropped into `inbox/`. A document that can be read but not copied, like a paste, has its figures read straight away and its missing bundle copy tracked as Outstanding.
